# Streamlit Community Cloud installs from this file when the app's main
# entrypoint is streamlit_app/Home.py. Mirrors the runtime deps the
# dashboard needs at app load — NOT the dbt-build deps (those are in the
# repo-root requirements.txt and only invoked on cold start to seed the
# DuckDB file).
streamlit==1.39.0
altair==5.4.1
pandas==2.2.3
duckdb==1.1.3

# dbt is only used on cold start to (re)build the marts if the .duckdb
# file isn't already present in the deployment. Streamlit Cloud's image
# is ephemeral, so we always rebuild from the seed CSVs.
dbt-core==1.8.7
dbt-duckdb==1.8.4
