# Streamlit Community Cloud installs from this file. Pins are ranges
# rather than exact versions so the resolver can pick whatever has a
# manylinux wheel for the active Python (3.11 per .python-version).
#
# The lower bounds matter:
#   streamlit >= 1.42 — first version with the `width=` API on
#     altair_chart / dataframe / etc. that this dashboard uses.
streamlit>=1.42,<2
altair>=5.4,<6
pandas>=2.2,<3
duckdb>=1.1,<2

# dbt is only used on cold start to (re)build the marts if the .duckdb
# file isn't already present in the deployment. Streamlit Cloud's image
# is ephemeral, so we always rebuild from the seed CSVs.
dbt-core>=1.8,<2
dbt-duckdb>=1.8,<2
