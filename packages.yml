packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
    # The maintained fork is now `metaplane/dbt_expectations`; calogica still
    # works but emits a deprecation notice. Switch when convenient.
  - package: dbt-labs/codegen
    version: [">=0.12.0", "<0.13.0"]
